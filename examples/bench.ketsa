// Ketsa Benchmark: Fibonacci
// Tests VM and JIT performance

func fib(n: int): int {
    if (n <= 1) {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

print "Computing fib(35)..."
let result = fib(35)
print "fib(35) = " + toString(result)
print "Done."
